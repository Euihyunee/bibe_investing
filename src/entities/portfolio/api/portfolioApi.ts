/**
 * @deprecated 이 파일은 더 이상 사용되지 않습니다.
 * 모든 데이터베이스 작업은 src/features/portfolio/api/portfolioActions.ts 의 Server Actions를 사용하세요.
 * 
 * Client Side에서의 import로 인한 'dns' 모듈 에러 방지를 위해 내용을 비웠습니다.
 * 이 파일의 타입(HoldingInput 등)은 src/features/portfolio/types.ts 로 옮겨졌습니다.
 */

export const IS_DEPRECATED = true
